const Loading = () => {
  return (
    <div>
      <img
        src={require('../../assets/loading.gif')}
        alt='Loading'
        style={{ height: '300px' }}
      />
    </div>
  );
};

export default Loading;
